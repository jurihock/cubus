using System;

namespace Cubus
{
  /// <summary>
  /// Indicates a read only cube,
  /// which may throw an exception
  /// on the write access.
  /// </summary>
  public interface IReadOnlyCube
  {
  }
}
